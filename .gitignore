/target
**/target
Cargo.lock
.DS_Store

# Local-only: planes, specs, docs de LLM/agentes (no rastreados)
/specs/

# Frontend build artifacts (reproduce with: npm --prefix crates/pass-tauri/ui run build)
crates/pass-tauri/ui/node_modules/
crates/pass-tauri/ui/dist/

# tsbuild output from composite tsconfig
crates/pass-tauri/ui/.tsbuild-node/

# Zola build output for the landing page (reproduce with: zola --root site build)
site/public/
